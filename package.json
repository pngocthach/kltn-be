{
  "private": true,
  "name": "kltn",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently --kill-others-on-fail \"cd packages/server && yarn dev\"  \"cd packages/web && yarn dev\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
